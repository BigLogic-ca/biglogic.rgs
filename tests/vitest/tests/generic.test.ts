import { test } from 'vitest'

// Use .todo for tests you intend to write later
test.todo('should handle empty or null values', () => {
  // Empty for now
})

// Use .skip to temporarily skip a block
test.skip('skipped test', () => {
  // Empty for now
})
